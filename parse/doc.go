// Package parse converts user-facing block, mask, and persisted block-state
// strings into Dragonfly world values.
//
// It is shared by command, schematic, and brush adapters so block identity
// and JSON property normalisation stay consistent.
package parse
